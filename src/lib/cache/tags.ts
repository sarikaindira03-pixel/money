// // lib/cache/tags.ts — typed tag getter for fetch calls
// import { CacheRegistry } from "./registry";

// type EntityKey = keyof typeof CacheRegistry;

// export function tag(entity: EntityKey, id?: string): string {
//   return CacheRegistry[entity].tag(id);
// }
